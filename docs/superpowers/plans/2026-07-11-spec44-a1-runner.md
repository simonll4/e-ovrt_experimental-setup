# Spec 44 A1 — Manifiesto paraguas + cliente control-plane + runner (orquestación)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** El **runner reproducible** de spec 44 §3 (ADR-004): lee un manifiesto paraguas `experiment.manifest.v1`, genera/toma el `experiment_id`, y dispara los dos planos por HTTP **en el orden correcto** (control-plane primero en `live`, su 201 implica suscripto; media-plane primero en `DBE-replay`), hace polling de ambos, y propaga fallas por exit code. Esta es la mitad de orquestación; la consolidación (ADR-014) y el generador de reporte van en el Plan A2.

**Architecture:** Repo `e-ovrt_experimental-setup`, único package Python `eovrt_webconsole` (`webconsole/backend/src/`, src-layout, setuptools, Py3.11+, venv Py3.14). Se reusa el patrón del `RunBackend` async httpx (`run_backend.py`) — un wrapper delgado sobre `httpx.AsyncClient` con taxonomía de errores — para el media-plane, y se agrega un **cliente análogo del control-plane** (`:8081`, endpoints de spec 41 §5). Los tests siguen el patrón existente: un **fake service ASGI** por plano (`make_fake_service` para el media-plane ya existe en `tests/fake_service.py`; se agrega uno para el control-plane) inyectado por `httpx.ASGITransport`. El runner es código nuevo bajo `eovrt_webconsole/experiment/`, sin estado propio.

**Tech Stack:** Python 3.11+, Pydantic v2, httpx (async), pyyaml, pytest + pytest-asyncio, ruff. NO se agregan dependencias nuevas.

## Global Constraints

- **Nunca commitear sin pedido explícito del usuario en ese turno.** Los pasos "Commit" preparan el commit; se ejecutan sólo si el usuario lo pide. Si no, `git add -A` y dejar el árbol sin `git commit`. (SDD usa `git write-tree` entre tareas; eso no es un commit.)
- **Nunca `Co-Authored-By`. Nada en GitHub; todo local.**
- **El repo ya tiene trabajo sin commitear** (webconsole, rama `feature/webconsole`, ~27 archivos, 151 tests verdes). **No tocar ni romper** ese trabajo: el runner es código NUEVO en módulos nuevos (`eovrt_webconsole/experiment/`). La suite completa (los 151 + los nuevos) debe quedar verde.
- **Orden de disparo live, no negociable (spec 40 §3.2 / doc 50 §5.1):** `POST :8081/api/runs` (`mode: live`) **primero** — su 201 implica que el `BusSource` ya está suscripto — y **después** `POST :8080/api/runs` con `bus.enabled: true`. En `DBE-replay` el orden se invierte: media-plane primero, esperar a que termine, luego control-plane en `mode: replay` apuntando al `detections.jsonl` del run.
- **Configs por payload (ADR-009):** el runner lee los YAML de config por plano del manifiesto y los **POSTea como JSON** (no por referencia de archivo del lado del servicio). Inyecta `experiment_id` en ambos, y en `live` fuerza `bus.enabled`/`alert_bus.enabled` y el orden.
- **Sin relojes en tests:** `experiment_id = exp_<UTC>Z_<slug>` — la función de generación recibe un `now: datetime` inyectable (default `datetime.now(timezone.utc)`), para que los tests pasen un instante fijo. Nunca `datetime.now()` hardcodeado dentro de la lógica testeada.
- `ruff` `line-length = 100`. Comentarios/docstrings en español, sin tildes ni ñ dentro del código.
- **Entorno:** `/home/simonll4/projects/e-ovrt_experimental-setup/webconsole/backend/.venv/bin/python`.
- **Tests:** `cd webconsole/backend && .venv/bin/python -m pytest -q`. **Lint:** `.venv/bin/python -m ruff check src tests`.
- **Baseline MEDIDA (2026-07-11):** `pytest -q` → **151 passed**. Cada tarea afirma end = baseline + tests nuevos.

---

## Task 1: Schema del manifiesto paraguas `experiment.manifest.v1` + `experiment_id`

**Files:**
- Create: `webconsole/backend/src/eovrt_webconsole/experiment/__init__.py`, `experiment/manifest.py`
- Test: `webconsole/backend/tests/test_experiment_manifest.py`

**Interfaces:**
- Produces: `PlaneRun(service: str, config: str, mode: str)`; `ExperimentManifest(schema_version: Literal["experiment.manifest.v1"], slug, experiment_id: str | None, runs: dict[str, PlaneRun], sequencing: str = "control_first", report: dict, frozen: dict)`; `generate_experiment_id(slug: str, now: datetime) -> str` → `exp_<YYYYMMDDTHHMMSSZ>_<slug>`; `load_manifest(path) -> ExperimentManifest`.

- [ ] **Step 1: Escribir el test que falla**

```python
from datetime import datetime, timezone

def test_generate_experiment_id_format():
    from eovrt_webconsole.experiment.manifest import generate_experiment_id
    got = generate_experiment_id("d1-fase1", datetime(2026, 7, 12, 14, 0, 0, tzinfo=timezone.utc))
    assert got == "exp_20260712T140000Z_d1-fase1"

def test_manifest_parses_umbrella_schema():
    from eovrt_webconsole.experiment.manifest import ExperimentManifest
    m = ExperimentManifest.model_validate({
        "schema_version": "experiment.manifest.v1", "slug": "d1",
        "runs": {"media": {"service": "http://localhost:8080", "config": "./media_run.yaml", "mode": "run"},
                 "control": {"service": "http://localhost:8081", "config": "./control_run.yaml", "mode": "live"}},
        "sequencing": "control_first", "report": {"output": "report/"},
        "frozen": {"prompt_set": "eind_v1", "pattern_set": "cr01_cr02_v2", "model_ref": "gdino-tiny"}})
    assert m.runs["control"].mode == "live" and m.sequencing == "control_first"

def test_manifest_rejects_unknown_sequencing():
    import pytest
    from eovrt_webconsole.experiment.manifest import ExperimentManifest
    with pytest.raises(ValueError):
        ExperimentManifest.model_validate({
            "schema_version": "experiment.manifest.v1", "slug": "d1",
            "runs": {"media": {"service": "u", "config": "c", "mode": "run"},
                     "control": {"service": "u", "config": "c", "mode": "replay"}},
            "sequencing": "media_first_wrong", "report": {}, "frozen": {}})
```

- [ ] **Step 2: Correr para verificar que falla** — Run: `.venv/bin/python -m pytest tests/test_experiment_manifest.py -q` — Expected: FAIL (ModuleNotFoundError).

- [ ] **Step 3: Implementación** (`experiment/manifest.py`)

```python
"""Manifiesto paraguas del experimento (ADR-004, spec 44 §2)."""
from __future__ import annotations
from datetime import datetime
from pathlib import Path
from typing import Literal
import yaml
from pydantic import BaseModel, ConfigDict, Field

class PlaneRun(BaseModel):
    model_config = ConfigDict(extra="forbid")
    service: str
    config: str          # ruta al YAML de config por payload de ese plano
    mode: str            # media: "run"; control: "live" | "replay"

class ExperimentManifest(BaseModel):
    model_config = ConfigDict(extra="forbid")
    schema_version: Literal["experiment.manifest.v1"]
    slug: str
    experiment_id: str | None = None
    runs: dict[str, PlaneRun]
    sequencing: Literal["control_first", "media_first"] = "control_first"
    report: dict = Field(default_factory=dict)
    frozen: dict = Field(default_factory=dict)

def generate_experiment_id(slug: str, now: datetime) -> str:
    return f"exp_{now.strftime('%Y%m%dT%H%M%SZ')}_{slug}"

def load_manifest(path: str | Path) -> ExperimentManifest:
    data = yaml.safe_load(Path(path).read_text(encoding="utf-8"))
    return ExperimentManifest.model_validate(data)
```

- [ ] **Step 4: Correr — deben pasar** — Expected: PASS (3).
- [ ] **Step 5: Suite completa** — Run: `.venv/bin/python -m pytest -q` — Expected: 154 passed (151 + 3).
- [ ] **Step 6: Commit (sólo si el usuario lo pidió)** — `git add -A && git commit -m "feat(experiment): schema experiment.manifest.v1 + experiment_id"`

---

## Task 2: Cliente HTTP del control-plane (`ControlPlaneBackend`)

Espejo async del `RunBackend` (media-plane) para los endpoints del control-plane (spec 41 §5, doc 38): `POST /api/runs` (`mode: live|replay`), `GET /api/runs/{id}`, `GET /api/runs/current`, `GET /api/runs/{id}/alerts`, `GET /api/config`.

**Files:**
- Create: `webconsole/backend/src/eovrt_webconsole/experiment/control_backend.py`
- Test: `webconsole/backend/tests/test_control_backend.py`, `tests/fake_control_service.py` (fake ASGI del control-plane)

**Interfaces:**
- Consumes: patrón de `run_backend.py` (`ServiceUnavailable`, `RunBusy`, `ServiceRejected`, `UnknownRun`).
- Produces: `ControlPlaneBackend(http: httpx.AsyncClient)` con `launch(config: dict, mode: str, experiment_id: str | None) -> str` (→ `control_run_id`; 409→`RunBusy(active_run_id)`, 422→`ServiceRejected`), `status(control_run_id) -> dict`, `current() -> dict` (incluye `subscribed`), `alerts(control_run_id) -> list[dict]`, `config() -> dict`. `make_fake_control_service(state)` (réplica mínima del control-plane :8081 para tests).

- [ ] **Step 1: Escribir el test que falla**

```python
import httpx, pytest
from eovrt_webconsole.experiment.control_backend import ControlPlaneBackend, RunBusy
from tests.fake_control_service import make_fake_control_service, FakeControlState

@pytest.fixture
def control_backend():
    state = FakeControlState()
    transport = httpx.ASGITransport(app=make_fake_control_service(state))
    http = httpx.AsyncClient(base_url="http://control", transport=transport)
    return ControlPlaneBackend(http), state

async def test_launch_live_returns_control_run_id_and_subscribed(control_backend):
    backend, state = control_backend
    run_id = await backend.launch({"input": {"type": "bus"}}, mode="live", experiment_id="exp-1")
    assert run_id
    current = await backend.current()
    assert current["subscribed"] is True   # el 201 de live implica suscripto

async def test_launch_conflict_raises_run_busy(control_backend):
    backend, state = control_backend
    state.active_run_id = "otro"
    with pytest.raises(RunBusy):
        await backend.launch({"input": {"type": "bus"}}, mode="live", experiment_id="x")
```

- [ ] **Step 2: Correr — falla** (ModuleNotFoundError).

- [ ] **Step 3: Implementación.** Copiar la estructura de `run_backend.py` (excepciones, `_get_json`, error mapping) a `control_backend.py`, con los métodos de arriba. `launch` hace `POST /api/runs` con `{"mode": mode, "config": config, "experiment_id": experiment_id}` (ajustar al shape real que acepta el router del control-plane — ver `e-ovrt_control-plane/src/eovrt_control/service/run_request.py` / `routers/runs.py`), devuelve `response.json()["control_run_id"]`. `make_fake_control_service` (en `tests/fake_control_service.py`): FastAPI mínimo con `POST /api/runs` (201 `{control_run_id}` salvo `state.active_run_id` → 409 `{active_run_id}`; al aceptar `mode:live` marca `state.subscribed=True`), `GET /api/runs/current` (200 `{progress, subscribed}`), `GET /api/runs/{id}` (200 `{summary,...}` o 404), `GET /api/runs/{id}/alerts` (200 lista), `GET /api/config` (200 `{effective_config}`), `/healthz`/`/readyz`.

- [ ] **Step 4: Correr — pasan** (2).
- [ ] **Step 5: Suite completa** — Expected: 156 passed (154 + 2).
- [ ] **Step 6: Commit (sólo si el usuario lo pidió)** — `git add -A && git commit -m "feat(experiment): cliente HTTP del control-plane + fake service"`

---

## Task 3: Runner — modo `DBE-replay` (media primero → control replay)

**Files:**
- Create: `webconsole/backend/src/eovrt_webconsole/experiment/runner.py`
- Test: `webconsole/backend/tests/test_runner_dbe_replay.py`

**Interfaces:**
- Consumes: `ExperimentManifest`, `RunBackend` (media), `ControlPlaneBackend`, `generate_experiment_id`.
- Produces: `async run_experiment(manifest, *, media_backend, control_backend, now, poll_interval_s=0.0, timeout_s=300.0, load_config=...) -> ExperimentResult`; `ExperimentResult(experiment_id, media_run_id, control_run_id, media_status, control_status, ok: bool)`. En DBE-replay: POST media (config + experiment_id) → poll hasta `succeeded`/`failed` → construir el input `replay` del control apuntando al `detections.jsonl` del media run → POST control (`mode: replay`) → poll. Falla temprana con `ok=False` y el status que corresponda.

- [ ] **Step 1: Escribir el test que falla** — un `run_experiment` con un manifiesto `sequencing: media_first` (o `runs.control.mode == "replay"`): media fake corre y termina `succeeded`, el runner arma el input replay y dispara el control fake, que termina `succeeded`. Asserts: `result.ok`, `result.experiment_id.startswith("exp_")`, ambos run_ids presentes, y que el control se disparó **después** de que el media terminó (el fake control registra el orden). Incluir un caso: media falla ⇒ `result.ok is False` y el control NO se dispara.

- [ ] **Step 2: Correr — falla.**

- [ ] **Step 3: Implementación** del orquestador DBE-replay (leer configs con `load_config` inyectable — default `yaml.safe_load` de `manifest.runs[plane].config` resuelto relativo al manifiesto; inyectar `experiment_id`; polling con `poll_interval_s` y `timeout_s`; el input replay del control se deriva del `runs/<media_run_id>/detections.jsonl` — para el test, el fake media reporta un `detections_path` en su summary o se construye por convención y el fake control lo acepta).

- [ ] **Step 4: Correr — pasan.**
- [ ] **Step 5: Suite completa** — Expected: 151 baseline + tareas previas + nuevos.
- [ ] **Step 6: Commit (sólo si el usuario lo pidió).**

---

## Task 4: Runner — modo `live` (control primero, suscripción antes del disparo)

**Files:**
- Modify: `webconsole/backend/src/eovrt_webconsole/experiment/runner.py`
- Test: `webconsole/backend/tests/test_runner_live.py`

**Interfaces:**
- Produces: en `run_experiment`, la rama `live` (`sequencing: control_first`, `runs.control.mode == "live"`): POST control (`mode: live`) **primero** → su 201 (y `current().subscribed is True`) implica suscripto → recién ahí POST media con `bus.enabled: true` + `experiment_id` → poll ambos hasta cierre (control cierra por `run_finished` 1:1) → `ExperimentResult`.

- [ ] **Step 1: Escribir el test que falla** — manifiesto `control_first`/`live`: el fake control registra el instante de suscripción y el fake media el instante de disparo; asserts: **suscripción del control ANTES del disparo del media** (aserción de orden, no de timing), ambos terminan, `result.ok`. Mutación-friendly: un test que verifique que si se invierte el orden (media primero) el runner lo rechaza o el gate lo detecta.

- [ ] **Step 2-6:** análogo a Task 3 (fail → impl la rama live → pass → suite → commit guarded). El orden es la invariante: el POST al control DEBE preceder al POST al media, y el runner NO dispara el media hasta confirmar `subscribed`/201.

---

## Task 5: **Gate** — runner e2e sobre los dos fakes + verificación por mutación

**Files:**
- Test: `webconsole/backend/tests/test_runner_gate.py`

- [ ] **Step 1: Escribir el gate** — corre `run_experiment` end-to-end en AMBOS modos (DBE-replay y live) sobre los fakes de ambos planos, y asserta: `experiment_id` generado y propagado a los payloads de ambos planos (el fake registra el `experiment_id` recibido), orden correcto por modo, ambos run_ids, `result.ok`.

- [ ] **Step 2: Correr el gate** — Expected: PASS.

- [ ] **Step 3: Verificar que el gate es significativo (mutación) — obligatorio.** Un gate que no puede fallar no es un gate. Dos mutaciones, una por vez, reportar salida literal:
  1. En `runner.py` rama live, disparar el media **antes** del control (invertir el orden). Esperado: **falla** la aserción de orden del gate (suscripción antes del disparo). Revertí.
  2. En `runner.py`, no inyectar `experiment_id` en el payload del media. Esperado: **falla** la aserción de propagación del `experiment_id`. Revertí.
  Si alguna mutación no hace fallar el gate, es vacuo: arreglalo.

- [ ] **Step 4: Suite completa + lint** — Run: `.venv/bin/python -m pytest -q && .venv/bin/python -m ruff check src tests` — Expected: verde total; `All checks passed!`.
- [ ] **Step 5: Commit (sólo si el usuario lo pidió).**

---

## Cierre

- [ ] **Registrar la deuda / lo que sigue:** el Plan A2 (consolidación ADR-014 + generador de reporte Tabla D.6 con el join `t_capture→alert`) consume los `ExperimentResult`/run_ids de este runner. El runner live real (contra los servicios de verdad, no fakes) se ejercita en el gate e2e de A2 o del ítem 6 completo.
- [ ] **Verificación final:** pegar `pytest -q` (verde), `ruff check` (limpio), y la salida literal de las dos mutaciones del gate.
- [ ] **Nota:** el `[project.scripts]` para un CLI del runner (`eovrt-experiment run <manifiesto>`) puede agregarse acá o en A2; si se agrega, un test mínimo que invoque `main([...])`.

## Alineación con spec 44 §3 (self-review)

| Requisito §3 | Task |
|---|---|
| Lee manifiesto, genera/toma `experiment_id` | 1, 3 |
| `POST` control-plane primero en live (suscripto antes del disparo) | 2, 4 |
| `POST` media-plane (config por payload + `experiment_id`) | 3, 4 |
| Polling de estado en ambos, timeout, exit code / `ok` | 3, 4 |
| Modo DBE-replay (media primero, control replay sobre `detections.jsonl`) | 3 |
| Consolidación (ADR-014) + reporte | **Plan A2** (siguiente) |
