# Broker del modulo de distribucion de alertas (spec 45 / ADR-016). Uso de
# laboratorio single-host, sin exposicion externa -- mismo criterio ya
# aceptado para el socket de Docker montado en la consola (ver README.md).
listener 1883
allow_anonymous true
persistence false
